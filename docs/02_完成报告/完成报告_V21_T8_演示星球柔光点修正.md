# V21 T8 演示星球柔光点修正完成报告

生成时间：2026-05-09

## 本次目标

把“演示星球”和知识球节点从“带实体光圈的点”改成“自己发亮的柔光点”。节点写得越多越亮，写得越少越灰暗，避免出现一圈一圈的廉价光环。

## 改动内容

1. `panel.js`
   - 移除真实知识节点外层实体 halo 圆。
   - 移除演示星球匿名点的 `.sphere-demo-glow` 外圈圆。
   - 新增 `earthNodeOpacity()`：按亮度等级控制节点透明度。
   - 新增 `earthNodeGlowFilter()`：按亮度等级选择柔光滤镜。
   - 调整 `earthNodeFill()`：
     - 高频节点：暖白 / 金色高亮。
     - 中频节点：金色。
     - 低频节点：灰棕色。
   - `earthNodeStrokeColor()` 改为 `none`，`earthNodeStrokeWidth()` 改为 `0`，不再用描边制造“圈”。
   - `earthSphereDefsHtml()` 新增 4 级柔光滤镜：
     - `node-glow-dim`
     - `node-glow-soft`
     - `node-glow-mid`
     - `node-glow-strong`
   - 演示点改为单个 core circle + SVG filter 发光，不再画实体外圈。

2. `panel.html`
   - 清理 `.sphere-demo-glow` 样式引用。
   - 保留 `.sphere-demo-core` 的稳定 SVG 渲染样式。

3. `tests/earth_sphere_fixture.html`
   - 同步演示点样式，保证截图 fixture 和真实面板一致。

4. `tests/panel_map.test.js`
   - 更新节点视觉测试：
     - 高频节点亮度高于低频节点。
     - 节点描边为 `none` / `0`。
     - 使用柔光滤镜。
     - 不再渲染实体 halo 圆。
   - 演示星球测试增加断言：`.sphere-demo-glow` 数量必须为 0。

## 验收标准

1. 节点外围不能再出现实心光圈。
2. 节点亮度按写作频次变化：多写更亮，少写更灰。
3. 演示星球只显示匿名柔光点，不出现外圈圆。
4. 不改数据库 schema。
5. 不改 Markdown 写入/解析格式。
6. 自动化测试全通过。

## 实际验收

- Chrome headless 测试：`96 passed / 0 failed`
- 截图：`screenshots_V21_T8_demo_sphere/demo_sphere_soft_glow_5nodes.png`
- 截图结果：演示星球已变成柔光点，不再有实体外圈。

## 未改动清单

- 未改 IndexedDB schema。
- 未改 DB migration。
- 未改 AI prompt。
- 未改 Markdown 格式。
- 未改真实节点数据计算逻辑，只改视觉表达。

## 备注

本机没有 `node` 命令，无法运行 `node --check`。本次继续以 Chrome headless 打开 `tests/test.html` 做真实浏览器验收。
