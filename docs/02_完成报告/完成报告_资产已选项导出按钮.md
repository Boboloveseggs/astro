# 完成报告：资产已选项导出按钮

时间：2026-05-10 11:13

## 本次问题

资产页已有「全选」和「删除」后，还需要在后面补一个「导出」按钮，方便把已勾选的素材、立意、作品导出。

## 本次改动

1. `panel.js:1297-1310`
   - 资产选择 UI 状态新增导出按钮联动。
   - 未勾选时隐藏；勾选后显示为「导出 N 项」。

2. `panel.js:1359-1381`
   - `bindAssetBatchControls()` 绑定 `assetBatchExportBtn`。

3. `panel.js:1385-1448`
   - 新增 `buildSelectedAssetsExportPayload()`。
   - 新增 `downloadJsonFile()`。
   - 新增 `exportSelectedAssets()`。
   - 导出格式为 JSON：
     - `export_format: "zhijing_selected_assets_v1"`
     - `articles`: 已选作品/速记
     - `clips`: 已选素材/立意，以及已选作品关联的素材/立意

4. `panel.js:1503`
   - 工具栏批量操作区在「全选」「删除」后新增「导出」按钮。

5. `panel.html:1494-1499`
   - 新增导出按钮的金色样式。

6. `tests/panel_assets.test.js:40, 278, 302`
   - 测试导出按钮默认隐藏、勾选后显示。
   - 新增 payload 测试，确认已选作品会带上关联素材/立意。

## 验收标准

| 标准 | 结果 |
| --- | --- |
| 导出按钮放在全选和删除后面 | 通过 |
| 没有勾选时导出按钮隐藏 | 通过 |
| 勾选后显示「导出 N 项」 | 通过 |
| 已选作品导出时带上关联素材/立意 | 通过 |
| 不改 DB schema，不改 Markdown 格式 | 通过 |
| 测试仍全过 | 通过，`116 passed / 0 failed` |

## 自查

- `rg "importScripts|WEEKLY_REPORT_ALARM|chrome\\.alarms|球球" background.js panel.js panel.html tests`：无匹配。
- Chrome Headless + DevTools Protocol 打开 `tests/test.html`：`116 passed / 0 failed`。

## 回滚路径

如需回滚，只需要恢复：

- `panel.js` 中导出按钮绑定和 `buildSelectedAssetsExportPayload()` / `downloadJsonFile()` / `exportSelectedAssets()`。
- `panel.html` 中 `.asset-batch-export` 样式。
- `tests/panel_assets.test.js` 中导出相关断言。

本次只增加前端导出能力，不影响已有数据。
