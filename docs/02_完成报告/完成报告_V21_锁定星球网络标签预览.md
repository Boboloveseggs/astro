# 完成报告：V21 锁定星球网络标签预览

## 本次目标

把锁定知识星球后的放大预览改成用户参考图那种关系网络：节点放大、节点之间有连线、节点名直接显示；再次点击后仍进入原来的知识球内部进行操作。

## 实际改动

### `panel.js`

- 扩展 `collectUniversePreviewNodes(ball)`：
  - 继续读取真实 `nodes_hit / wikilinks`。
  - 同时把 `new_concepts / tags` 作为低权重补充节点，避免预览太空。
  - 见 `panel.js:2357`。
- 重做 `universeKnowledgePreviewTexture(ball)` 的视觉：
  - 贴图提升到 `1024px`，拉近后文字更清晰。
  - 主节点放在中心，周围节点按网络布局展开。
  - 节点之间绘制暗金色连线。
  - 节点名称直接画在贴图上，中心节点字体更大。
  - `userData.labelCount` 记录标签数量，见 `panel.js:2586`。
- 锁定态预览尺寸放大：
  - 从 `Math.max(88, baseScale * 2.20)` 改为 `Math.max(154, baseScale * 3.10)`。
  - 见 `panel.js:3184`、`panel.js:3377`。

### `tests/panel_map.test.js`

- 新增断言：锁定预览必须包含可读节点标签 `labelCount > 0`。
- 更新断言：锁定预览尺寸必须足够大，`scale.x >= 154`。
- 见 `tests/panel_map.test.js:212-213`。

## 验收标准对照

| 用户要求 | 当前结果 |
| --- | --- |
| 放大后像参考图那种关系网络 | 已改成节点 + 连线 + 标签的网络预览 |
| 预览时上面要有字 | 已直接在贴图上绘制节点名称 |
| 先预览，点进去后再操作 | 首次点击锁定并展示预览，再次点击进入原知识球 |
| 不要动原来的宇宙 | 未改宇宙全景星云参数 |

## 测试结果

Chrome Headless + DevTools Protocol 打开 `tests/test.html`：

```text
97 passed / 0 failed
```

## 截图留档

目录：`screenshots_V21_preview_network/`

- `locked_network_preview.png`：锁定后的网络标签预览。

## 不动清单

- 未改 IndexedDB schema。
- 未改 Markdown 写入格式。
- 未改 AI prompt。
- 未改宇宙全景星云参数。
- 未删除 `universePlanetTexture()` fallback。

## 备份

- 已生成 zip：`C:\参赛文档\backups\zhijing_v4_preview_network_20260509_1609.zip`
