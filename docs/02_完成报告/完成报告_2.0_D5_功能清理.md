# 完成报告 2.0 D5：功能清理与入口收束

完成时间：2026-05-08  
执行依据：用户本轮 7 项核查清单 + `任务下发_2.0冲刺_九日清单.md` Day 5 功能清理日  
性质：删除冗余、隐藏入口、移动入口、修复 D1 遗留视觉 bug

## 做了什么

1. 删除旧 `renderMap()`、旧 `renderKnowledgeSphere()`、旧 `reviewTimeline()`，不再保留死代码。
2. 删除旧 D3 force 图相关残留：`mapSimulation`、`#mapSvg` 样式、`d3.min.js` 面板脚本引用。
3. 删除旧「自由图谱 / 知识球」mode 残留：`mapMode` 变量和 `.map-mode-*` CSS。
4. 删除 B3 视图保存入口，并进一步删除资产 toolbar 的 `assetViewSelect`、`assetViewPanel`、筛选按钮和相关前端函数。
5. 保留 `asset_views` 数据层与备份格式：`db.js`、`options.js` 仍会导出/导入旧视图配置，避免破坏用户旧备份。
6. 把 A1 本地文件夹入口移入 options 的「高级设置」折叠区。
7. 把 Obsidian Vault 导出移入 options 的「高级设置」折叠区。
8. 把资产 Tab toolbar 的「节点」入口移到地图 Tab 右下角浮按钮，文案为「全局节点」。
9. `cardTags(r)` 改成固定 `return '';`，分析详情页不再显示标签卡。
10. 资产列表底部标签也不再渲染；tags 字段仍保留在数据和搜索里。
11. 修复 D1 留下的 `count-badge` 空徽标问题：0 篇时 `display:none`，有数量时显示。
12. 增加 Day5 清理回归测试。

## 用户 7 项核查结果

| 核查项 | 结果 |
|---|---|
| 旧 `renderMap` / `renderKnowledgeSphere` / `reviewTimeline` 是否真的删除 | 已删除，grep 无残留 |
| B3 视图保存入口是否真的从 toolbar 删除 | 已删除，并删除 `assetViewSelect` / `assetViewPanel` 前端 UI |
| A1 本地文件夹 + Vault 导出是否真的移到 options 高级折叠区 | 已移动到 `advancedSettings` |
| 标签卡 `cardTags(r)` 是否真的 `return ''` | 是，函数体固定返回空字符串 |
| 资产 Tab toolbar 的「节点」按钮是否真的移到地图 Tab 浮按钮 | 是，改为地图右下角 `全局节点` 浮按钮 |
| `count-badge` 0 时是否 `display:none` | 是，CSS 默认隐藏，`updateAssetCount()` 0 时设为 `none` |
| 测试是否仍然全过 | 是，`55 passed / 0 failed` |

## 涉及哪些文件

### `panel.js`

- 第 447-449 行：`cardTags(r)` 固定 `return '';`。
- 第 497-500 行：`updateAssetCount()` 根据数量切换 `assetCount` 的 `display`。
- 第 543-557 行：资产 toolbar 只渲染搜索框和本地扫描按钮；删除视图选择、筛选面板、保存视图和节点按钮。
- 第 600-610 行：资产搜索仍保留 tags 字段参与检索，但不渲染 tag chip。
- 第 900 行：地图 Tab 内新增 `map-node-fab` 浮按钮「全局节点」。
- 第 1021-1024 行：浮按钮点击后切到资产 Tab 并打开节点库。
- 第 1703-1705 行：`reviewTimelineBlock()` 只返回 `renderTimelineDeck(items)`，不再保留旧列表视图分支。
- 第 1849 行：复盘 Tab 继续接入时间轴卡片牌组。

### `panel.html`

- 第 392-395 行：`.count-badge` 默认 `display:none`。
- 第 671-678 行：新增 `.map-node-fab` 地图右下角浮按钮样式。
- 第 800-822 行：资产 toolbar 样式只保留搜索与扫描按钮所需样式。
- 已删除：旧 `#mapSvg`、`.map-mode-*`、旧 `.timeline-dot`、旧 `.asset-tag*`、旧 `.asset-view-*`、旧 `.review-mode-*` CSS。
- 已删除：`d3.min.js` 面板脚本引用。

### `options.html`

- 第 132-147 行：新增「高级设置」折叠区样式。
- 第 194-201 行：主区只保留 JSON 数据备份 / 恢复。
- 第 205-224 行：本地文件夹与 Obsidian Vault 导出移入「高级设置」折叠区。

### `tests/`

- `tests/panel_map.test.js` 第 44-45 行：增加地图「全局节点」浮按钮断言。
- `tests/panel_cleanup.test.js`：新增 2 个清理回归测试。
- `tests/test.html` 第 38 行：接入 `panel_cleanup.test.js`。

## 保留的数据层

- `db.js` 仍保留 `asset_views` 的导出/导入白名单。
- `options.js` 仍在 JSON 备份 / 恢复状态里统计 `asset_views`。
- `tests/db.test.js` 仍覆盖 `asset_views` 备份兼容性。

这意味着：前端入口被清理，但旧备份格式不会坏。

## 验收命令与结果

浏览器测试：

```text
tests/test.html => 55 passed / 0 failed
```

fixture 渲染：

```text
tests/timeline_deck_fixture.html => tl-deck-layout / tl-card-slider / tl-date-gear 正常渲染
```

重复 id 检查：

```text
panel.html duplicate ids: none
options.html duplicate ids: none
tests/test.html duplicate ids: none
tests/timeline_deck_fixture.html duplicate ids: none
```

残留 grep：

```text
renderMap / renderKnowledgeSphere / reviewTimeline / assetView / saveViewBtn / nodeIndexBtn / mapMode / d3.min / #mapSvg => 无残留
```

## 不动清单

- 不改 IndexedDB schema。
- 不改 Markdown 写入格式。
- 不改 JSON 备份格式。
- 不改 A1 本地同步的数据合并逻辑。
- 不改 Obsidian Vault 导出函数。
- 不改 AI prompt 和 tags 数据字段。
- 不改 D2/D3 3D 地球核心算法、自转、hover 卡。
- 不改 D4 时间轴 deck 核心渲染。

## 风险预警

1. B3 视图保存入口已从前端删除，但旧 `asset_views` 数据仍会随备份保留。这是刻意保留的兼容层，不是 UI 残留。
2. 资产 toolbar 现在只剩搜索框和重新扫描按钮；任务表里提到的「素材 / 立意 / 作品」sub-tab 尚未在当前版本落地，本次没有新增空入口。
3. Node.js 和 git 在当前终端 PATH 不可用，因此本次没有使用 `node --check` 或 `git diff`；验收以 Chrome headless 测试、fixture 渲染、重复 id 检查和 grep 为准。

## Claude 审核清单 8 问

1. 本次改动是否触碰数据写入、删除、导入、同步或迁移？  
   没有。本次只改前端入口、渲染、CSS 和测试。

2. 是否可能造成旧数据被覆盖、丢失、重复、孤立引用？  
   不会。本次不写入数据库，不删除 IndexedDB 数据。

3. 是否维护了 `articles` 与 `nodes` 的一致性？  
   本次不触碰两表关系。节点库入口只是移动，不改变节点数据生成和反链逻辑。

4. 是否改变了旧 Markdown 格式或 JSON 备份格式？  
   没有。`asset_views` 仍被备份兼容。

5. 是否改变了 IndexedDB schema？如果改变，迁移路径是什么？  
   没有改变 schema，不需要迁移。

6. 是否影响 A1-A5 已有能力？  
   A1 本地同步和 A5 Vault 导出能力保留，只是入口移动到高级设置；A2 搜索仍可搜 tags；A3 tags 数据仍保留但前端隐藏；A4 测试通过。

7. 自动化测试能覆盖什么？不能覆盖的手动验收是什么？  
   自动化覆盖旧函数删除、`cardTags()` 隐藏、`count-badge` 隐藏、地图浮动节点按钮、D4 时间轴仍可渲染。手动需在扩展面板检查 options 高级折叠区、地图右下角浮按钮跳转节点库。

8. 如果三天后 Claude 只看完成报告，是否能复现验收？  
   可以。报告列出文件、行号、grep 项、测试结果和不动清单。

## 回滚路径

如需撤回本次清理：

1. 从本报告对照 `panel.js` 恢复资产视图 UI、旧函数或旧入口。
2. 从 `options.html` 把本地文件夹和 Vault 导出移回主区。
3. 从 `tests/test.html` 移除 `panel_cleanup.test.js`。
4. 删除 `tests/panel_cleanup.test.js`。

