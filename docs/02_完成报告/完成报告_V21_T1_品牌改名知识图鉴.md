# V2.1 T1 完成报告：品牌改名为知识图鉴

执行依据：`任务下发_V21_自用版迭代清单.md` T1。

## 做了什么

1. 将非历史文件中的品牌文案「知境图鉴」替换为「知识图鉴」。
2. 更新扩展名称、侧边栏标题、设置页标题、README、Demo 脚本、测试页标题。
3. 更新 `panel.js / db.js / background.js / content.js` 的 debug/warn/error 前缀。
4. 保留 `DB_NAME = 'zhijing_db'`、`BroadcastChannel('zhijing_updates')`、测试 DB 前缀 `zhijing_db_test_*` 不变。
5. 历史完成报告、历史任务下发和问题日志未修改。

## 涉及文件 + 行号

- `manifest.json`：第 3 行 `name`，第 5 行 `description`，第 57 行 `default_title`。
- `panel.html`：第 5 行 `<title>`，第 1233 行 `<h1>`。
- `options.html`：第 5 行 `<title>`，第 152 行 `<h1>`。
- `README.md`：第 1-3 行。
- `docs/2.0_demo_script.md`：第 1、9、79、83 行。
- `panel.js`：第 71、73、331、1071、2448 行日志前缀。
- `db.js`：第 746、763、796、1027、1062 行品牌文案或日志前缀；第 3 行 `DB_NAME` 保持 `zhijing_db`。
- `background.js`：第 25、29、34、47、52、54、57、64、100、104、143 行日志前缀。
- `content.js`：第 117 行日志前缀。
- `options.js`：第 166 行备份文件名；第 205、223 行 `zhijing_updates` 保持不变。
- `tests/test.html`：第 5、22 行测试页标题。

## 1.0 引用

无。T1 是品牌文案替换，不涉及 1.0 视觉或交互移植。

## 验收标准与实际结果

| 验收标准 | 实际结果 |
|---|---|
| panel 顶部标题显示「知识图鉴」 | 已完成 |
| 设置页标题显示「知识图鉴」 | 已完成 |
| 扩展管理页名称为「知识图鉴」 | `manifest.json` 已完成 |
| debugLog 前缀改为「知识图鉴」 | 已完成 |
| IndexedDB 名仍为 `zhijing_db` | 已保留 |
| BroadcastChannel 仍为 `zhijing_updates` | 已保留 |
| 历史报告和问题日志不改 | 已保留 |

## 测试新增数 + 总数

- T1 未新增自动化测试，按清单要求使用 grep 验证。
- 本批次最终全量测试结果：`85 passed / 0 failed`。

## grep 验证

- 非历史源码、README、docs、tests 中已无旧品牌名「知境图鉴」。
- 历史报告、历史任务下发、问题日志中仍保留旧名称，符合 T1 不动清单。
- `zhijing_db` / `zhijing_updates` 仍存在，符合兼容要求。

## 不动清单

- 不改 IndexedDB schema。
- 不改 `DB_NAME = 'zhijing_db'`。
- 不改 `BroadcastChannel('zhijing_updates')`。
- 不改测试 DB 前缀。
- 不改历史完成报告、历史任务下发、问题日志。
- 不改文章 Markdown 结构；只改品牌相关标题/文件名文案。

## 风险预警

1. `dbExportObsidianVault()` 生成的索引文件名已随品牌改为 `知识图鉴索引.md`，属于 T1 品牌改名范围；文章 Markdown front-matter 和正文结构未改变。
2. 用户已有导出文件夹里可能仍存在旧的 `知境图鉴索引.md`，新导出会生成新索引文件，不会删除旧文件。

## Claude 8 问

1. 是否触碰数据写入、删除、导入、同步或迁移？  
   未触碰 IndexedDB 数据写入/删除/迁移；只改了品牌文案和本地导出索引文件名文案。
2. 是否可能造成旧数据被覆盖、丢失、重复、孤立引用？  
   不会。数据库名和频道名保持不变。
3. 是否维护 `articles` 与 `nodes` 一致性？  
   本轮不改节点关系。
4. 是否改变旧 Markdown 格式或 JSON 备份格式？  
   JSON 结构不变；文章 Markdown 结构不变。备份文件名和 Vault 索引标题随品牌改名。
5. 是否改变 IndexedDB schema？  
   没有。
6. 是否影响 A1-D9 已有能力？  
   不影响核心流程，只改显示文案。
7. 自动化测试覆盖什么？不能覆盖的手动验收是什么？  
   T1 用 grep 验证；手动验收为打开 panel、options、扩展管理页查看新名称。
8. 三天后 Claude 只看完成报告，是否能复现验收？  
   可以，按报告中的 grep 和手动页面检查即可。

## 回滚路径

把本轮列出的非历史文件中「知识图鉴」替换回「知境图鉴」，但继续保留 `zhijing_db` 和 `zhijing_updates` 不变。
