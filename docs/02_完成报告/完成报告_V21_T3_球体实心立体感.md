# V2.1 T3 完成报告：球体实心立体感

执行依据：`任务下发_V21_自用版迭代清单.md` T3。

## 做了什么

1. 在 3D 地球 SVG `<defs>` 中新增两个 radialGradient：
   - `earthSphereBody`：中心暖、边缘暗的球体主体渐变。
   - `earthSphereGloss`：左上角微弱高光。
2. 在经纬线图层前插入两个填充圆层：
   - `earthSphereBodyLayer`
   - `earthSphereGlossLayer`
3. 将经纬线透明度弱化，让它们成为辅助参考线。
4. 同步更新 `tests/earth_sphere_fixture.html`，避免后续截图 fixture 与真实面板样式分裂。
5. 新增测试确认 SVG 已渲染主体渐变、高光渐变和两个填充层。

## 涉及文件 + 行号

- `panel.js`
  - 第 1538-1547 行：新增 `earthSphereBody` 和 `earthSphereGloss`。
  - 第 1556-1557 行：新增主体填充层和高光层。
- `panel.html`
  - 第 855-859 行：经纬线 opacity 调低为 0.20 / 0.15。
- `tests/earth_sphere_fixture.html`
  - 第 112-116 行：同步经纬线 opacity。
- `tests/panel_map.test.js`
  - 第 74-81 行：新增实心球体渐变和高光层测试。

## 1.0 引用

无直接代码引用。T3 按 V2.1 下发文档提供的 radialGradient 方案实现，用于恢复 1.0 深色金色视觉的“球体感”。

## 验收标准与实际结果

| 验收标准 | 实际结果 |
|---|---|
| 球看起来实心，中心暖、边缘暗 | 已加入主体 radialGradient |
| 左上角有微弱高光 | 已加入 gloss radialGradient |
| 经纬线弱化，不喧宾夺主 | opacity 已降到 0.20 / 0.15 |
| 节点仍清晰可辨 | 填充层位于节点和连接层下方 |
| 球面 SVG 仍可拖拽旋转 | 未改拖拽/旋转状态机 |

## 测试新增数 + 总数

- 新增测试：1 个。
- 本批次最终全量测试：`85 passed / 0 failed`。

## 不动清单

- 不改球节点位置算法。
- 不改拖拽、自转、缩放、hover 卡。
- 不改地图数据结构。
- 不改 IndexedDB schema。

## 风险预警

本轮只验证 SVG 结构存在，未生成新的视觉截图。Claude 视觉验收时仍应打开地图 Tab 或 fixture 看球体观感，确认渐变没有压暗节点。

## Claude 8 问

1. 是否触碰数据写入、删除、导入、同步或迁移？  
   没有。
2. 是否可能造成旧数据被覆盖、丢失、重复、孤立引用？  
   不会。
3. 是否维护 `articles` 与 `nodes` 一致性？  
   本轮不改数据。
4. 是否改变旧 Markdown 格式或 JSON 备份格式？  
   没有。
5. 是否改变 IndexedDB schema？  
   没有。
6. 是否影响 A1-D9 已有能力？  
   不影响；只在地图 SVG 底层增加视觉填充层。
7. 自动化测试覆盖什么？不能覆盖的手动验收是什么？  
   自动化覆盖渐变定义与填充层存在；手动验收需要观察球是否更实心、经纬线是否弱化。
8. 三天后 Claude 只看完成报告，是否能复现验收？  
   可以，运行测试并打开地图 Tab 检查 SVG 与视觉效果。

## 回滚路径

删除 `earthSphereBody / earthSphereGloss` 两个 gradient 和两个填充 circle，把 `.sphere-shell-line / .sphere-shell-lat` opacity 恢复为 0.56 / 0.42，并移除对应测试。
