# V2.1 T2 完成报告：球节点金色调和

执行依据：`任务下发_V21_自用版迭代清单.md` T2。

## 做了什么

1. 新增地球专用节点色板 `EARTH_NODE_PALETTE`。
2. `earthTypeColor()` 改为读取地球专用金色调色板，不再使用 `TYPE_META.color`。
3. 地图图例改为使用地球色板，和球面节点颜色一致。
4. `TYPE_META` 保持原样，资产详情、节点反链等类型语义色不受影响。
5. 新增测试确认球面颜色已变为金色调，同时 `TYPE_META.person.color` 等语义色未被改动。

## 涉及文件 + 行号

- `panel.js`
  - 第 451-456 行：`TYPE_META` 保留原语义色。
  - 第 457-462 行：新增 `EARTH_NODE_PALETTE`。
  - 第 1439-1440 行：`earthTypeColor()` 使用地球色板。
  - 第 1443-1450 行：地图 legend 使用地球色板。
- `tests/panel_map.test.js`
  - 第 65-72 行：新增金色调色板与 `TYPE_META` 解耦测试。

## 1.0 引用

无直接代码引用。T2 是 V2.1 视觉调和，目标是让 3D 球回到 1.0 深色金色主题气质。

## 验收标准与实际结果

| 验收标准 | 实际结果 |
|---|---|
| 球节点统一为金色调家族 | 已完成 |
| 不同类型仍有色调差 | person / concept / theory / event 分别映射暖白金 / 主金 / 深金 / 橙金 |
| 资产详情类型 badge 保留 TYPE_META | 已保留，测试覆盖 |
| hover 卡类型 label 仍显示 | 未改 hover 卡类型逻辑，继续使用 `TYPE_META.label` |

## 测试新增数 + 总数

- 新增测试：1 个。
- 本批次最终全量测试：`85 passed / 0 failed`。

## 不动清单

- 不改 `TYPE_META` 原语义色。
- 不改分析数据结构。
- 不改 IndexedDB schema。
- 不改节点 hover 卡内容结构。

## 风险预警

金色调统一后，类型间差异会比原来的紫/绿/橙弱，主要差异转由亮度等级、半径、透明度和 hover 类型 label 承担。若用户后续觉得难辨认，可在 hover 卡中把类型 label 加粗或给 legend 增加说明。

## Claude 8 问

1. 是否触碰数据写入、删除、导入、同步或迁移？  
   没有。
2. 是否可能造成旧数据被覆盖、丢失、重复、孤立引用？  
   不会。
3. 是否维护 `articles` 与 `nodes` 一致性？  
   本轮只改显示颜色，不改节点数据。
4. 是否改变旧 Markdown 格式或 JSON 备份格式？  
   没有。
5. 是否改变 IndexedDB schema？  
   没有。
6. 是否影响 A1-D9 已有能力？  
   不影响；只调整地图球面的视觉色板。
7. 自动化测试覆盖什么？不能覆盖的手动验收是什么？  
   自动化覆盖 `earthTypeColor()` 和 `TYPE_META` 解耦；手动验收需要看 100 节点球整体是否偏金色。
8. 三天后 Claude 只看完成报告，是否能复现验收？  
   可以，运行测试并打开地图 Tab 观察节点色调即可。

## 回滚路径

删除 `EARTH_NODE_PALETTE`，把 `earthTypeColor()` 恢复为 `TYPE_META[type]?.color || '#5a8aaa'`，并移除对应测试。
