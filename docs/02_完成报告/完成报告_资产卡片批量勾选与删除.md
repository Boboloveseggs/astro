# 完成报告：资产卡片批量勾选与删除

时间：2026-05-10 11:09

## 本次问题

资产页里的「素材 / 立意 / 作品」都只能点进详情后删除，批量清理很麻烦。用户希望在外层卡片直接勾选，支持 Shift 连选、全选，并在右上角出现批量删除按钮。

## 本次改动

1. `panel.js:11`
   - 新增 `assetSelectionState`，统一记录当前资产页模式、可见条目、已选 ID 和 Shift 连选起点。

2. `panel.js:1260-1397`
   - 新增资产批量选择核心函数：
     - `resetAssetSelection()`
     - `syncAssetSelectionScope()`
     - `selectedAssetItems()`
     - `assetSelectButtonHtml()`
     - `updateAssetSelectionUI()`
     - `bindAssetSelection()`
     - `bindAssetBatchControls()`
     - `deleteSelectedAssets()`
   - Shift 点击会把上一次勾选位置到当前卡片之间的所有卡片选中。
   - 批量删除时：
     - 素材/立意走 `dbDeleteClip()`。
     - 作品/速记走 `dbDeleteArticle()`，沿用原有连带删除素材/立意、清节点引用的安全逻辑。

3. `panel.js:1422-1440`
   - 资产工具栏右侧新增：
     - 已选数量提示。
     - 「全选 / 取消全选」按钮。
     - 勾选后才出现的「删除 N 项」按钮。

4. `panel.js:1493-1538`
   - 素材/立意卡片右下角新增金色圆形勾选按钮。
   - 选中状态会保留在当前可见列表内。

5. `panel.js:1542-1598`
   - 作品/速记卡片右下角新增同样的勾选按钮。
   - 点击卡片正文仍进入详情，点击勾选按钮不会误打开详情。

6. `panel.html:564-589`
   - 新增卡片选中态和右下角金色圆形勾选按钮样式。

7. `panel.html:1431-1499`
   - 工具栏改为可换行布局，右侧新增批量操作区。
   - 批量删除按钮使用偏危险色，和普通按钮区分。

8. `tests/panel_assets.test.js:38-294`
   - 更新工具栏测试：确认全选与批量删除按钮存在。
   - 更新素材卡测试：确认卡片右下角有勾选按钮。
   - 新增批量选择测试：确认右下角勾选、Shift 连选、批量删除三条作品的流程。

## 验收标准

| 标准 | 结果 |
| --- | --- |
| 素材卡右下角有金色圆形勾选按钮 | 通过 |
| 立意卡沿用同一套勾选能力 | 通过 |
| 作品卡右下角有金色圆形勾选按钮 | 通过 |
| 勾选后右上角出现批量删除按钮 | 通过 |
| Shift 往下勾选可连选范围内卡片 | 通过 |
| 全选按钮可一键选择当前可见列表 | 通过 |
| 批量删除不改 DB schema | 通过 |
| 测试仍全过 | 通过，`115 passed / 0 failed` |

## 与预期相比

本次先完成“批量选择 + 批量删除 + 全选”的基础层。你提到“方便导出之类的”，我没有顺手加导出按钮，因为这会牵涉导出格式和目标范围，适合单独做一轮；但现在选择状态已经有了，后续接“导出已选”会更顺。

## 自查

- `rg "importScripts|WEEKLY_REPORT_ALARM|chrome\\.alarms|球球" background.js panel.js panel.html tests`：无匹配。
- Chrome Headless + DevTools Protocol 打开 `tests/test.html`：`115 passed / 0 failed`。

## 回滚路径

如需回滚，只需要恢复：

- `panel.js` 中资产批量选择状态、工具栏按钮、卡片勾选和 `deleteSelectedAssets()`。
- `panel.html` 中卡片勾选与批量操作样式。
- `tests/panel_assets.test.js` 中新增/更新的批量选择断言。

本次没有修改 IndexedDB schema，也没有修改 Markdown 写入格式。
