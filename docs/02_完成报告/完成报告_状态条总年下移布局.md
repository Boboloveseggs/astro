# 完成报告：状态条总年下移布局

完成时间：2026-05-10

## 本次目标

顶部状态条在窄宽度下，五个统计项同时挤在一行会打架。周目标进度条缩短后右侧有空位，因此把“总”和“年”移到周目标进度右侧。

## 改动内容

- 文件：`panel.html`
- 顶部状态条布局从一行 5 列调整为：
  - 第一行：`月 / 周 / 日`
  - 第二行：`本周目标进度 / 总 / 年`
- `.prog-track` 继续保持短进度条，长度从 86px 调整为 54px，给“总 / 年”留空间。
- 统计数值增加 `text-overflow: ellipsis`，窄宽度下不会硬挤到相邻块。

## 保留内容

- 文案仍为：`本周目标达成 ✓`
- 每周达标篇数下拉仍保留。
- 不改 `panel.js` 的统计逻辑。
- 不改 service worker、抓取链路、地图/宇宙、DB schema、Markdown 写入格式。

## 验证

- 全量测试：`113 passed / 0 failed`
- 静态检查：
  - 无 `球球` 残留。
  - 未重新引入 `importScripts / chrome.alarms / WEEKLY_REPORT_ALARM`。
