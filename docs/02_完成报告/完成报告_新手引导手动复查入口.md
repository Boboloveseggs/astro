# 完成报告：新手引导手动复查入口

完成时间：2026-05-10 20:18

## 问题

用户已经不是第一次安装，自动首次引导不会再弹出，但需要先人工检验一遍完整新手引导；检验完后仍然希望它保持关闭，不要以后每次自动出现。

## 修复内容

1. `panel.html`
   - 在顶部开灯按钮旁新增一个很小的 `?` 按钮：
     - `id="onboardingReplayBtn"`
     - title：`重看新手引导`
   - 这个按钮只用于手动复查，不参与首次自动弹出判断。

2. `panel.js`
   - 新增 `replayPanelOnboarding()`。
   - 点击 `?` 后从第 1 步重新播放完整面板新手引导。
   - 不清空 `onboardingSeen` / `onboardingComplete`，所以看完后仍然不会自动弹。

3. `options.html`
   - 在设置页第一次使用提示下新增：
     - `重看 API 设置引导`

4. `options.js`
   - 新增 `replayOptionsOnboarding()`。
   - 用户在设置页也可以单独重看 API Key 设置引导。

5. `tests/panel_cleanup.test.js`
   - 新增测试确认 `replayPanelOnboarding()` 可以手动启动引导。

## 验收标准

| 标准 | 结果 |
| --- | --- |
| 不是每次自动弹出 | 通过 |
| 用户可以手动重看完整新手引导 | 通过 |
| 重看不会重置首次出现状态 | 通过 |
| 设置页可以单独重看 API 设置引导 | 通过 |
| 不改 DB schema / Markdown 格式 / 地图宇宙逻辑 | 通过 |
| 全量测试 | 通过，`123 passed / 0 failed` |

## 视觉验证

截图：

```text
C:\参赛文档\zhijing_v4\screenshots_onboarding\panel_onboarding_replay_button.png
```

## 自查

静态检查：

```text
rg -n "importScripts|WEEKLY_REPORT_ALARM|chrome\.alarms|球球" background.js panel.js panel.html tests
无命中
```

测试：

```text
123 passed / 0 failed
```

## 备份

```text
C:\参赛文档\backups\zhijing_v4_20260510_2018.zip
```
