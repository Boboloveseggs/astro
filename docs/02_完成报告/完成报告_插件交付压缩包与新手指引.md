# 完成报告：插件交付压缩包与新手指引

时间：2026-05-11

## 本次目标

把当前知识图鉴打包成一个别人拿到后可以解压安装的浏览器扩展压缩包，并把 4 张安装引导图整理成 Word 文档《插件新手指引.docx》放入压缩包。

## 实际改动

1. 新增内置演示数据
   - 新增：`demo_data/知识图鉴_演示数据_当前库.json`
   - 数据来自本机真实扩展 IndexedDB 导出。
   - 实际导出内容：5 篇文章、14 个节点、16 条素材。
   - 注：本机可找到的真实库是 5 篇，不是口头预期的 10 篇；没有硬凑假数据。

2. 空库首次打开自动导入演示数据
   - 修改：`panel.js`
   - 新增常量：
     - `DEMO_SEED_KEY`
     - `DEMO_DATA_PATH`
   - 新增函数：
     - `seedDemoDataIfEmpty()`
   - 初始化顺序调整为：
     - 先做旧 storage 迁移
     - 再检查空库并导入演示数据
     - 再初始化项目、资产计数和工具栏
   - 保护规则：
     - 已有文章时不导入
     - 已导入过时不重复导入
     - 不覆盖用户已有数据

3. 新增回归测试
   - 修改：`tests/panel_cleanup.test.js`
   - 新增 2 个用例：
     - 空库首次打开会导入打包内置演示数据
     - 非空库不会 fetch demo 数据，也不会调用 `dbImportAll`

4. 生成 Word 新手指引
   - 新增：`release_assets/插件新手指引.docx`
   - 新增源文案：`release_assets/插件新手指引_源文案.md`
   - 内容包括：
     - 解压压缩包
     - 打开扩展管理页
     - 开启开发者模式
     - 加载“知识图鉴插件”文件夹
     - 固定插件图标
     - API Key 安全提醒
     - 第一次打开后的使用入口说明
   - 已嵌入 4 张图片：
     - `新手教程1.png`
     - `新手教程2.png`
     - `新手教程3.png`
     - `新手教程4.png`

5. 生成最终交付包
   - 目录：`C:\参赛文档\知识图鉴_插件交付包_20260511`
   - 压缩包：`C:\参赛文档\知识图鉴_插件交付包_20260511.zip`
   - 压缩包根目录包含：
     - `插件新手指引.docx`
     - `README_先看我.txt`
     - `知识图鉴插件/`
     - `演示数据备份/知识图鉴_演示数据_当前库.json`

6. 生成项目备份
   - 备份压缩包：`C:\参赛文档\backups\zhijing_v4_release_package_20260511_1408.zip`
   - 备份排除了本轮临时目录 `.tmp_*`

## 验收标准与结果

| 验收项 | 标准 | 实测结果 |
| --- | --- | --- |
| 压缩包可解压 | 解压后能看到 Word 指引和插件文件夹 | 通过 |
| 插件文件完整 | `manifest.json`、核心 JS、HTML、icons、demo_data 都在 | 通过 |
| 浏览器可加载 | 用 Edge `--load-extension` 加载交付包内 `知识图鉴插件` | 通过，service worker 正常出现 |
| 空库自动导入 | 新 profile 打开 `panel.html` 后自动导入演示数据 | 通过，`dbGetArticleCount()` 返回 5 |
| 不覆盖用户数据 | 非空库测试不触发 fetch / import | 通过 |
| 单元测试 | 现有测试不能回退 | 通过，128 passed / 0 failed |
| Word 指引包含图片 | DOCX 内含 4 张教程图 | 通过，结构校验存在 4 个 media 图片 |

## 验证记录

- 浏览器单元测试：`128 passed / 0 failed`
- 交付包加载检查：
  - Edge 成功加载 `C:\参赛文档\知识图鉴_插件交付包_20260511\知识图鉴插件`
  - service worker：`chrome-extension://.../background.js`
- 演示数据自动导入检查：
  - 新 profile 打开面板后：`count = 5`
  - `demoSeededFromPackage20260511 = true`
- ZIP 反查：
  - `插件新手指引.docx` 存在
  - `知识图鉴插件/manifest.json` 存在
  - `知识图鉴插件/demo_data/知识图鉴_演示数据_当前库.json` 存在
  - `演示数据备份/知识图鉴_演示数据_当前库.json` 存在

## 未改动清单

- 未改 IndexedDB schema
- 未改 Markdown 写入格式
- 未改宇宙 / 知识球视觉参数
- 未删除当前本机已有文章数据
- 未修改 AI prompt

## 备注

本机没有 LibreOffice / soffice，无法按 Documents 渲染器流程把 DOCX 转成 PNG 逐页视觉验图。本次采用 DOCX OOXML 结构校验、图片资源校验和打包反查作为替代。文档可继续用 Word 打开做人工视觉确认。

## 回滚路径

如需撤回本次交付改动：

1. 删除 `demo_data/知识图鉴_演示数据_当前库.json`
2. 删除 `release_assets/插件新手指引.docx`
3. 删除 `release_assets/插件新手指引_源文案.md`
4. 删除 `release_assets/README_先看我.txt`
5. 从 `panel.js` 移除 `DEMO_SEED_KEY`、`DEMO_DATA_PATH`、`seedDemoDataIfEmpty()` 和初始化里的调用
6. 从 `tests/panel_cleanup.test.js` 移除 2 个 demo seed 测试
7. 删除交付目录和压缩包：
   - `C:\参赛文档\知识图鉴_插件交付包_20260511`
   - `C:\参赛文档\知识图鉴_插件交付包_20260511.zip`
