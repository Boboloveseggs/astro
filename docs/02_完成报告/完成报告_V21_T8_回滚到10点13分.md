# 完成报告：V21 T8 回滚到 10 点 13 分版本

## 本次目标

按 `任务下发_紧急_回滚到10点13分版本.md` 执行，只把 11:41 后改坏宇宙视觉的参数回拨到 10:13 状态，不重做结构。

## 实际改动

### `panel.js`

| 检查项 | 11:41 后偏离状态 | 已恢复到 10:13 |
| --- | --- | --- |
| 程序星云底图尺寸 | `1280 x 800` | `2400 x 1500`，见 `panel.js:2391-2392` |
| 星云底图调色 | 掺回蓝青色冷色粒子 | 紫黑金冷色粒子，`palette: 'purple-black-gold'`，见 `panel.js:2479` |
| 星点分布 | 紧凑 4 臂/小半径分布 | 10:13 的 5 臂大半径分布，见 `panel.js:2484` |
| 螺旋云粒子层 | 保留检查 | `createUniverseSpiralArmCloud(22000, pointTexture)`，见 `panel.js:3029` |
| 主螺旋臂数量 | 保留检查 | `armCount = 2`，见 `panel.js:2578` |
| 星云面透明度 | 保留检查 | `opacity: 0.44`，见 `panel.js:2634` |
| 跨星球连线透明度 | 保留检查 | `opacity: 0.004`，见 `panel.js:3096` |
| 锁定星球缩放 | `0.82 / 0.78` | `0.68`，见 `panel.js:2779`、`panel.js:3119-3120` |
| 锁定星球贴图 | `sprite.userData.previewTexture` | `state.planetTexture`，见 `panel.js:2928` |
| 锁定星球混合模式 | 保留检查 | `THREE.NormalBlending`，见 `panel.js:2929`、`panel.js:3129` |
| 锁定星球中景尺寸 | 紧凑预览 | `Math.max(96, baseScale * 2.80)`，见 `panel.js:2941`、`panel.js:3133` |

核心函数均保留：

- `universePlanetTexture()`：`panel.js:2258`
- `releaseUniverseLockToOverview()`：`panel.js:2700`
- `createUniverseSpiralArmCloud()`：`panel.js:2575`

### `tests/panel_map.test.js`

- 同步修正 11:41 后留下的紧凑预览断言：从 `lockedObject.scale.x <= 90` 改为 10:13 的中景锚点 `lockedObject.scale.x >= 96`，见 `tests/panel_map.test.js:210`。
- 这是测试语义回拨，不是新增功能。

## 验证截图

参考图：

- `screenshots_V21_T8_palette_planet/purple_gold_overview.png`
- `screenshots_V21_T8_palette_planet/locked_planet_midshot_final2.png`

本次回滚后截图：

- `screenshots_V21_rollback_compare/fixed_overview_18balls.png`
- `screenshots_V21_rollback_compare/fixed_overview.png`
- `screenshots_V21_rollback_compare/fixed_locked_midshot.png`

截图结论：全景已恢复紫黑金星云、规则螺旋粒子带和金色知识星球点；不再出现 11:41 后的低分辨率星云底图、蓝青冷色粒子和紧凑锁定预览。

## 测试结果

Chrome Headless + DevTools Protocol 打开 `tests/test.html`：

```text
97 passed / 0 failed
```

说明：第一次测试出现 `95 passed / 2 failed`，原因是 `panel_map.test.js` 仍保留 11:41 后的紧凑预览断言，导致 10:13 中景缩放被误判。同步测试锚点后全量通过。

## 不动清单

- 未改 IndexedDB schema。
- 未改 Markdown 写入格式。
- 未改 AI prompt / 追问对话 / 复盘 Tab / 资产 Tab 功能逻辑。
- 未删除 10:13 核心函数。
- 未把 `palette: 'purple-black-gold'` 改成其他色调。

## 备份

- 已生成 zip：`C:\参赛文档\backups\zhijing_v4_rollback_done_20260509_1536.zip`
- 备份内容：`panel.js`、`panel.html`、`tests/panel_map.test.js`、本完成报告。

## Claude 8 问

1. `universePlanetTexture()` 还在吗？  
   在，`panel.js:2258`。
2. `releaseUniverseLockToOverview()` 还在吗？  
   在，`panel.js:2700`，滚轮向后退回全景逻辑仍保留。
3. `createUniverseSpiralArmCloud()` 还在吗？  
   在，`panel.js:2575`。
4. 星云底图是否回到 `2400 x 1500`？  
   是，`panel.js:2391-2392`。
5. 螺旋粒子是否仍为 `22000` + 2 条主臂？  
   是，`panel.js:3029` 和 `panel.js:2578`。
6. 锁定星球是否使用 `planetTexture + THREE.NormalBlending`？  
   是，`panel.js:2928-2929`、`panel.js:3128-3129`。
7. 锁定缩放是否回到 `0.68`？  
   是，`panel.js:2779`、`panel.js:3119-3120`。
8. 测试和截图是否已完成？  
   是，测试 `97 passed / 0 failed`，截图保存在 `screenshots_V21_rollback_compare/`。
