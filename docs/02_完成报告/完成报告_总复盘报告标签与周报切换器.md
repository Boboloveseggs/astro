# 完成报告：总复盘报告标签与周报切换器

时间：2026-05-10 11:01

## 本次问题

1. 地图后面的独立标签写成了「指纹」，不符合产品语气，应改成「总复盘报告」。
2. 周复盘虽然已保存历史，但历史入口藏在卡片底部，用户不容易发现“可以翻看其他周”。

## 本次改动

1. `panel.html:1781`
   - 顶部标签从「指纹」改为「总复盘报告」。
   - 内部 `data-tab="fingerprint"` 暂时保留，避免大范围改动事件逻辑。

2. `panel.html:635-692`
   - 新增周报标题行、上一周/下一周按钮、周报下拉框、历史周报标题的样式。

3. `panel.js:4798-4829`
   - 新增 `weeklyReportEntries()`：把当前周报与已保存周报合并排序。
   - 新增 `renderWeeklyReportSwitcher()`：在「第 X 周」旁边渲染“上一周 / 下拉选择 / 下一周”。
   - `renderWeeklyReportHistory()` 底部增加「历史周报」标题，入口更明确。

4. `panel.js:4833-4858`
   - `renderWeeklyReportCard()` 改为把周报切换器放在周标题旁边。
   - 仍保留底部历史列表，方便直接点某一周。

5. `panel.js:4913-4945`
   - `bindWeeklyReportHistory()` 支持三种切换方式：
     - 下拉框选择某一周。
     - 点击“上一周 / 下一周”。
     - 点击底部历史周报列表。

6. `tests/panel_review.test.js:107-112`
   - 测试补充：确认周标题旁有下拉框、上一周/下一周按钮、底部“历史周报”入口。

## 验收标准

| 标准 | 结果 |
| --- | --- |
| 地图后面的标签显示「总复盘报告」 | 通过 |
| 周标题旁能看到周报切换控件 | 通过 |
| 底部明确显示「历史周报」列表 | 通过 |
| 旧周报不删除，仍可回溯 | 通过，沿用完整 `weeklyReports` 存储 |
| 不改地图、宇宙、DB schema、Markdown 格式 | 通过 |
| 测试仍全过 | 通过，`114 passed / 0 failed` |

## 与预期相比

你后面补充说不必拘泥于右侧详情窗口，所以本次没有强行改成 1.0 的左右大布局，而是采用更适合侧边栏宽度的方案：周标题旁直接给切换器，底部再保留完整历史列表。这样既能翻周报，也不会把页面做得更挤。

## 自查

- `rg "importScripts|WEEKLY_REPORT_ALARM|chrome\\.alarms|球球" background.js panel.js panel.html tests`：无匹配。
- Chrome Headless + DevTools Protocol 打开 `tests/test.html`：`114 passed / 0 failed`。

## 回滚路径

如需回滚，只需要恢复：

- `panel.html` 中顶部标签文案与周报切换器样式。
- `panel.js` 中 `weeklyReportEntries()`、`renderWeeklyReportSwitcher()`、`renderWeeklyReportHistory()`、`renderWeeklyReportCard()`、`bindWeeklyReportHistory()` 的本次改动。
- `tests/panel_review.test.js` 中新增断言。

本次没有修改任何持久化数据结构，回滚不会影响已有周报记录。
