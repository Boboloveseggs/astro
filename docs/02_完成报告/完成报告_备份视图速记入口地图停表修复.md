# 备份视图、速记入口、地图停表修复完成报告

## 做了什么

### db.js
- `dbExportAll()` 将 JSON 备份格式升级为 `export_format: 2`。
- 新增 `config` 白名单导出：
  - `projects`
  - `asset_views`
- `dbImportAll()` 支持恢复 `config.projects` 和 `config.asset_views`。
- 新增 `normalizeBackupConfig(config)`：
  - 只允许可序列化、可迁移的配置进入 JSON 备份。
  - 明确排除 `local_folder` 文件夹句柄，避免备份里出现不可恢复对象。
  - 过滤格式异常的项目和保存视图。
- 旧备份仍可导入：没有 `config` 的旧 JSON 会按原逻辑恢复文章和节点。

### options.js
- JSON 导出成功提示增加项目数、视图数。
- JSON 导入成功提示增加项目数、视图数。

### panel.html
- 将速记入口移出 `waitingSection`。
- 检测到知乎文章后，`waitingSection` 会隐藏，但速记入口仍然保留在分析 Tab 中。
- 用户不需要先切手动，也能直接保存速记。

### panel.js
- 新增 `stopMapSimulation()`。
- 离开地图 Tab 时停止 D3 force simulation。
- 地图数据不足、显示空状态前，也会停止旧 simulation。
- `renderMapView()` 继续在重绘前停止旧 simulation，避免模式切换残留。

### tests/db.test.js
- 新增 2 个测试：
  - `normalizeBackupConfig: 只保留可备份的 config 白名单`
  - `normalizeBackupConfig: 忽略格式异常的视图配置`

### 文档
- 更新 `完成报告_A4_最小单元测试.md`：`db.test.js` 从 18 个用例变为 20 个，总测试数变为 29。
- 更新 `完成报告_B2_灵感快捕.md`：最新测试结果改为 `37 passed / 0 failed`。
- 更新 `完成报告_自查高优先级修复.md`：最新测试结果改为 `37 passed / 0 failed`。
- 更新 `问题日志_Claude审核清单.md`：记录 B3 保存视图已进入 JSON 备份。

## 验收标准

| 步骤 | 预期结果 |
|------|----------|
| 保存一个 Dataview 视图后导出 JSON | 备份文件包含 `config.asset_views` |
| 创建多个项目后导出 JSON | 备份文件包含 `config.projects` |
| 查看 JSON 备份 | 不包含 `local_folder` |
| 导入旧版 JSON 备份 | 仍可恢复文章和节点，不因缺少 `config` 报错 |
| 导入新版 JSON 备份 | 文章、节点、项目、保存视图都能恢复 |
| 打开检测到文章的分析页 | 速记入口仍然可见 |
| 离开地图 Tab | 旧的 D3 force simulation 停止 |
| 地图节点不足显示空状态 | 旧的 D3 force simulation 停止 |
| 打开 `tests/test.html` | 显示最新测试总数通过；当前为 `37 passed / 0 failed` |

## 与预期相比

- 补齐了 B3 的备份闭环：保存视图不再只留在当前浏览器 IndexedDB config 里。
- 补齐了 B2 的入口可达性：速记更接近“随手记”，不被文章检测状态挡住。
- 修掉了 B1 的隐藏性能边角：地图离开后不继续跑 simulation。
- 备份格式有小幅升级，但保持旧备份导入兼容。

## 涉及文件

- `db.js`
- `options.js`
- `panel.html`
- `panel.js`
- `tests/db.test.js`
- `完成报告_A4_最小单元测试.md`
- `完成报告_B2_灵感快捕.md`
- `完成报告_自查高优先级修复.md`
- `问题日志_Claude审核清单.md`

## 不动清单

- 不改变 IndexedDB schema。
- 不改变 AI prompt。
- 不改变文章、节点的数据结构。
- 不导出 `local_folder` 文件夹句柄。
- 不改变本地 Markdown 同步格式。
- 不改变 Obsidian Vault 导出格式。
- 不继续做 B4。

## 测试说明

- 已运行 Chrome headless：
  - `file:///C:/参赛文档/zhijing_v4/tests/test.html`
- 实际结果：
  - `37 passed / 0 failed`
- 已做静态检查：
  - `panel.html` 无重复 `id`。
  - 文档中的最新测试计数已统一为 29。
- 仍需手动验收：
  - 在扩展设置页实际导出/导入 JSON，确认保存视图恢复。
  - 在检测到文章的知乎页面确认速记入口可见。
  - 在地图 Tab 切换/离开时确认图谱交互没有异常。

## Claude 审核风险回应

- 本次触碰 JSON 备份/恢复，属于数据层风险，但没有改 IndexedDB schema。
- 新备份格式是向后兼容升级：旧备份缺少 `config` 时不会报错。
- 只导出配置白名单，避免把 File System Access API 的目录句柄写入 JSON。
- `asset_views` 恢复只影响资产 Tab 的保存视图，不改文章、不改节点、不改图谱数据。
- 地图停表只处理隐藏 simulation，不改变节点布局数据。
