# 紧急任务：回滚 panel.js 到 10:13 漂亮版本

> 编制人：Claude（架构 + 审核侧）
> 紧急原因：09:48 完成「螺旋星云与宇宙掌控感」、10:13 完成「宇宙配色收敛与锁定星球退回」之后，11:41 又做了一次改动，**没写完成报告**，**用户认为这次改动把宇宙改丑了**。
> 用户原话：**「10:14 我要这一版」**——明确要回到 10:13 那一版的视觉效果。
> 当前所有 10:13 的核心函数都还在 panel.js 里，未被删除。

## 1. 上下文

### 已确认仍存在的 10:13 关键代码

| 函数/参数 | 位置 | 状态 |
|----------|------|------|
| `universePlanetTexture()` | panel.js 第 2894 行 | ✅ 还在 |
| `releaseUniverseLockToOverview()` | panel.js 第 2570 行 | ✅ 还在 |
| `THREE.NormalBlending`（锁定对象）| 第 2799、2999 行 | ✅ 还在 |
| `palette: 'purple-black-gold'` | 第 2495 行 | ✅ 还在 |
| `planetTexture` 应用到锁定对象 | 第 2998-3004 行 | ✅ 还在 |

**结论**：11:41 那次改动**没删功能**，只是调了**视觉参数**（透明度/颜色细节/缩放系数/粒子数/某个 magic number）。

### 10:13 漂亮版的视觉锚点（截图 + 报告）

参考资源：
- `screenshots_V21_T8_palette_planet/purple_gold_overview.png` ← 紫黑金全景宇宙
- `screenshots_V21_T8_palette_planet/locked_planet_midshot_final2.png` ← 锁定星球中景
- `screenshots_V21_T8_palette_planet/planet_texture_preview.png` ← 程序星球贴图预览
- `完成报告_V21_T8_宇宙配色收敛与锁定星球退回.md` ← 详细描述了 10:13 所有改动

10:13 完成报告里写明的核心视觉特征：
1. **配色**：紫黑金（不要花花绿绿，特别是不要蓝青色）
   - `universeGalaxyTexture()` 冷色粒子从蓝青改成黑紫/暗紫
   - `createUniversePointCloud()` 远星 / haze / dust / bright 冷色全部改为紫系
   - `createUniverseSpiralArmCloud()` 标记 `palette: 'purple-black-gold'`
   - 跨星球连线改成暗紫色
2. **锁定星球**：
   - `material.map` 切到 `planetTexture`
   - 使用 `THREE.NormalBlending`（不是 AdditiveBlending）
   - 提高 `renderOrder`，让星球浮在背景前
   - 锁定状态压低背景星云、粒子层、核心光晕透明度
3. **锁定镜头**：`targetZoom = 0.68`（点击时拉近但不压满画面）
4. **滚轮退回**：锁定状态滚轮向后缩放 → `releaseUniverseLockToOverview` → 镜头 tween 回全景

## 2. 任务

### 步骤 1：截图当前丑版状态对比

GPT 自查：
- 用 fixture（`tests/universe_fixture.html`）渲染当前 panel.js 的宇宙效果
- 截图保存到 `screenshots_V21_rollback_compare/current_ugly.png`
- 与 `screenshots_V21_T8_palette_planet/purple_gold_overview.png` 对比

### 步骤 2：找出 11:41 那次改动的具体参数

**重点对比下面 8 个视觉参数**，看 11:41 是否动过它们：

| 参数 | 10:13 的值（参考完成报告/截图）| 当前 panel.js 的值 |
|------|---------------------------|------------------|
| `galaxyPlane.material.opacity` 基线 | 应该是 `0.44` 左右 | grep 实际值 |
| `createUniverseSpiralArmCloud` 的粒子数 | `22000` | grep 实际值 |
| 螺旋臂数量 | `2 条主螺旋臂` | 看 createUniverseSpiralArmCloud 内部 |
| 跨星球连线全景透明度 | `0.004` | grep 实际值 |
| 锁定星球 `targetZoom` | `0.68` | grep 实际值 |
| 普通星点贴图分辨率 | `256` | grep 实际值 |
| 核心柔光贴图分辨率 | `384` | grep 实际值 |
| 程序星云底图尺寸 | `2400 x 1500` | grep 实际值 |

如果上面 8 个里有任何一个**当前值 ≠ 10:13 的值**，**全部恢复到 10:13 的值**。

### 步骤 3：用户额外可能感受到丑的几个嫌疑点

11:41 没写报告，但根据用户"突然变丑"的反馈，可能改了：
- 中心光圈大小（10:13 故意压低过，11:41 可能又调大了）
- 星点粒子的颜色（10:13 是紫黑金，11:41 可能掺了别的色）
- 某层星云的不透明度（10:13 让螺旋粒子层为主视觉）
- 锁定星球 vs 全景的显示比例

**对照 `purple_gold_overview.png` 和 `locked_planet_midshot_final2.png` 两张截图**，凡是当前看起来不一样的地方，都是 11:41 改坏的。

### 步骤 4：禁止做的事

- ❌ 不允许删除 `universePlanetTexture()` / `releaseUniverseLockToOverview()` / `createUniverseSpiralArmCloud()` 等 10:13 的核心函数
- ❌ 不允许把 `palette: 'purple-black-gold'` 改成其他色调
- ❌ 不允许把锁定对象的 `THREE.NormalBlending` 改回 `AdditiveBlending`
- ❌ 不允许碰 IndexedDB / Markdown / JSON 备份格式
- ❌ 不允许碰追问对话 / 复盘 / 资产 Tab

### 步骤 5：写完成报告

`完成报告_V21_T8_回滚到10点13分.md` 必须含：
1. 当前丑版 vs 10:13 漂亮版的具体差异（每个改坏的参数列出来）
2. 改回的具体行号 + 参数值
3. 渲染验证截图（与 10:13 截图对比）
4. 测试通过数（应仍为 97 + 之前其他测试）
5. 不动清单
6. Claude 8 问

## 3. 验收标准

GPT 完成后，Claude 会做：
- 对比当前 panel.js 与 10:13 完成报告里描述的所有参数
- 看 GPT 提交的"修复后"截图与 `purple_gold_overview.png` / `locked_planet_midshot_final2.png` 是否视觉一致
- 如果**任何一个参数**仍偏离 10:13 状态，打回重做

## 4. 备份说明

Claude 已在执行此任务前用 zip 备份当前状态到：
`C:\参赛文档\backups\zhijing_v4_20260509_1300.zip`

GPT 也应在改动后再做一次备份：
```powershell
$timestamp = Get-Date -Format 'yyyyMMdd_HHmm'
Compress-Archive -Path 'c:\参赛文档\zhijing_v4\panel.js','c:\参赛文档\zhijing_v4\panel.html' -DestinationPath "C:\参赛文档\backups\zhijing_v4_rollback_done_$timestamp.zip" -Force
```

## 5. 时间预算

回滚任务工程量极小（只改若干视觉参数）：**0.5 天**。

GPT 立即执行。
