'use strict';

// Public GitHub build: no Zhihu OpenAPI credential is included.
// Fill these only in a private local build, never in committed source.
window.ZHIHU_OPENAPI_DEFAULTS = {
  enabled: false,
  appKey: '',
  appSecret: '',
};
